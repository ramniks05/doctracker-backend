spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/docutrack
    username: postgres
    password: postgres

app:
  jwt:
    secret: ${APP_JWT_SECRET:}
    access-token-ttl: 30m
  gemini:
    api-key: ${APP_GEMINI_API_KEY:}
    model: ${APP_GEMINI_MODEL:gemini-2.5-flash-lite}
